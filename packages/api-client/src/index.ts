// The generated OpenAPI client (schema.d.ts, client.ts) lands here in
// CQ-004/CQ-005 via `make api-client`. This placeholder export keeps the
// package buildable/importable/lintable in the meantime.
export const placeholder = true;
