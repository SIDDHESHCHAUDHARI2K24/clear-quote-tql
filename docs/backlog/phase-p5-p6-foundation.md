# P5/P6 foundation — Implementation plan

Shared groundwork for the P5 (LO Tier B, CQ-025–030) and P6 (Borrower Tier
B/C, CQ-031–034) parallel build, per `docs/backlog/phase-p5-p6-plan.md`
(H3, E1–E8, E14, E15, E17). No backlog item or Kaneo task owns this file;
it is the wave-1 unit (`p56-foundation`, slot 11) that lands before any
wave-2 worker starts. It contains no item features.

## Decisions & questions (stage 1)

| # | Type | Item | Resolution |
| --- | --- | --- | --- |
| 1 | Decision | Migration head | Decided: one migration `*_p5p6_foundation.py` with `down_revision = e419a34bcdbd` (the single head on `phase-p5-p6`). Every later P5/P6 migration chains off it. |
| 2 | Decision | `consents` pending rows | Decided: `text_hash`, `ip`, `at` become nullable (a pending request has no decision yet). New `status` enum `consent_status` (`pending`/`accepted`/`declined`/`expired`), `server_default='accepted'` so existing rows backfill as accepted, then the model default is `pending` for new rows. `requested_by` → `users.id` (indexed, `ON DELETE SET NULL`). Consent stays "append-only" in spirit: the decision fields are written once; CQ-033 owns the state machine. |
| 3 | Decision | `consents.expires_at` | Decided: stored, nullable; CQ-033 sets `requested_at + 14 d` and checks it on read (spec). The foundation adds no expiry logic. |
| 4 | Decision | `support_requests.reference` | Decided: `String(16)` unique, generated by CQ-034 (`SUP-XXXXX`). `application_id` FK `ON DELETE SET NULL`; `borrower_account_id` FK `ON DELETE CASCADE`. `topic`/`preferred_contact` are plain strings (validated by CQ-034's schema), not Postgres enums, to keep CQ-034 free to tweak the list without a migration. |
| 5 | Decision | `application_drafts` | Decided: `data` JSONB `NOT NULL DEFAULT '{}'`, `current_tab` `String` default `you`; partial unique index `uq_application_drafts_open_per_borrower` on `(borrower_account_id) WHERE submitted_application_id IS NULL`. Every FK column is indexed (`backend/tests/test_schema.py`). |
| 6 | Decision | `applications.source` | Decided: native enum `application_source` (`los`/`portal`), `server_default 'los'`, `NOT NULL`. `ApplicationSource` lives in `core/enums.py` (shared by the pipeline and CQ-032). |
| 7 | Decision | Flag messages (E8) | Decided: `flags.message` text nullable. `rules.py` gains `RULE_MESSAGES` + `flag_message(rule, field_key)` (static, human text per rule id, with a field label for `ob_required_field`, e.g. "Cannot price: missing Occupancy"). `write_flag(..., message=None)` stores the given message or falls back to `flag_message`; an existing unresolved row has its message refreshed. `run_and_persist` passes the rule's own dynamic `RuleResult.message` (e.g. "Only 14 months of housing history on file; 24 required."). The migration backfills existing rows with the same static text via SQL `CASE`. |
| 8 | Decision | Pipeline skip-import (E14) | Decided: a new activity `load_application_source(application_id) -> str` (reads `applications.source`) runs first; the workflow calls `import_application` only when the source is `los`. The DB stays the single source of truth (the caller that starts the workflow need not pass anything). The activity writes no event and no status, and is registered as an internal activity next to `record_pipeline_resumed` (the six contract activities and their log line are unchanged). |
| 9 | Decision | `least_loaded_lo_id` (E15) | Decided: new `features/applications/assignment.py::least_loaded_lo_id(db)` counts *active* applications (status not `withdrawn`/`closed`) per LO (role `lo`), ties broken by `full_name` then `id`. `auth/borrower/service.py` imports it as `_least_loaded_lo_id` (old call site unchanged). The borrower sign-up test that relied on client counts now gives the busy LO active applications (logged necessity). |
| 10 | Decision | `core/clock.py` | Decided: `now()` returns `settings.clock_now` (parsed ISO-8601; naive values are taken as UTC) or `datetime.now(UTC)`. Reads `get_settings()` each call, so tests monkeypatch settings. New P5/P6 code uses it; old code is not refactored (E2). |
| 11 | Decision | `core/storage.py` | Decided: thin boto3 wrapper; each function takes an optional `bucket` (default `settings.s3_bucket`) and an optional `client` (default a cached boto3 client from `s3_*` settings). Async API (`asyncio.to_thread`) because every caller is an async service: `ensure_bucket`, `put_object`, `get_object`, `presigned_get_url`, `delete_object`; `stream_object` returns a sync chunk iterator (Starlette `StreamingResponse` iterates sync iterators in a threadpool). `seed/generators/documents.py` is **not** refactored: it threads its own client and bucket through `seed_persona`, so the change is not trivial. |
| 12 | Decision | `core/pagination.py` | Decided: `Page[T]` (pydantic generic: `items`, `total`, `page`, `page_size`); `async paginate(db, stmt, page, page_size) -> Page[Any]` returns ORM rows / scalars as `items` (callers map to their schema), clamps `page_size` to 1..100 (default 25) and `page` to ≥ 1. Count via `select(func.count()).select_from(stmt.order_by(None).subquery())`. Services are async, so this is too. |
| 13 | Decision | Seed E17 | Decided: `seed.loader.seed_no_application_borrower(db)` creates client "Nadia Noapp" (`noapp.borrower@clearquote-demo.test`) assigned via `least_loaded_lo_id`, plus a verified borrower account, only when `SEED_BORROWER_PASSWORD` is set; idempotent. Called from `seed/reset.py` after the persona accounts. Personas and background data are untouched. |
| 14 | Decision | Valkey databases for slots ≥ 11 | Found: the shared Valkey runs with the default 16 databases, and slot N uses db N+2, so slots ≥ 14 (db ≥ 16) would fail. Decided: `infra/docker-compose.yml` starts Valkey with `--databases 64`; `scripts/worktree-env.sh` checks the live `databases` value and exits with a clear "recreate Valkey (`docker compose -f infra/docker-compose.yml up -d valkey`)" message if the slot's db is out of range. The shared container is **not** recreated by this unit (it would drop other sessions); the orchestrator does it once before wave 2. |
| 15 | Decision | LO console route group (E5) | Decided: `app/(staff)/layout.tsx` (client) wraps `StaffSessionProvider` + `StaffShell`. `/` and `applications/[id]/**` move under it (URLs unchanged). `/` becomes the CQ-025 stub; the old signed-in text moves to the user menu (name + role label). `page.test.tsx` moves with it and tests the stub; the session behaviours (401 → logout → `/login`, reject → `/login`, Sign out) move to `StaffSessionProvider.test.tsx`/`StaffShell.test.tsx`. |
| 16 | Decision | Borrower route group (E6) | Decided: `app/(portal)/layout.tsx` wraps `BorrowerSessionProvider` + `PortalShell`; `/`, `/support`, `/apply`, `/tasks/credit-check/[id]` live inside. `login`, `signup`, `gallery/**` and `report/[token]` stay outside (the report has its own chrome and e2e). On 401 the provider clears the stale cookie via logout, then `router.replace("/login?next=<current path>")` (omitting `next` for `/`, matching middleware). |
| 17 | Decision | UI primitives (E7) | Decided: focus-trap logic is extracted from `Overlay` into `packages/ui/src/utils/useFocusTrap.ts` and reused by `Overlay` and `Drawer` (Overlay's tests guard the refactor). `Toast` = `ToastProvider` + `useToast()` (`show({message, tone})`, auto-dismiss 4 s, `role="status"` region with `aria-live="polite"`). `MultiSelect` is a button (`aria-expanded`, `aria-haspopup="listbox"`-free: a `role="group"` checkbox list) that opens a popover; Esc and outside click close it. |

No big gaps.

## Why

The 12 wave-2/3 item units run in parallel worktrees. Anything two of them
would both need — a schema change, a helper, an app shell, a UI primitive —
lands here first so they never edit the same file or fork `alembic heads`.

## What changes

| Area | Files (create / modify) |
| --- | --- |
| Migration | create `alembic/versions/<rev>_p5p6_foundation.py`; modify `alembic/env.py` (import new models) |
| Models | modify `backend/app/features/borrower/consent/models.py`, `applications/verification/models.py`, `applications/models.py`, `core/enums.py`; create `features/portal/support/{__init__,models}.py`, `features/portal/apply/{__init__,models}.py` |
| Core helpers | create `backend/app/core/{clock,storage,pagination}.py` + tests in `backend/app/core/tests/`; modify `core/config.py` |
| Assignment | create `backend/app/features/applications/assignment.py` (+ test); modify `features/auth/borrower/service.py`, `auth/borrower/tests/test_signup.py` |
| Flag messages | modify `applications/verification/{rules,service}.py` (+ tests) |
| Pipeline | modify `backend/app/workflows/{activities,application_pipeline,worker}.py`; create `workflows/tests/test_portal_source_skips_import.py` |
| Seed | modify `seed/loader.py`, `seed/reset.py`; create `seed/tests/test_no_application_borrower.py` |
| LO console | move `apps/lo-console/src/app/{page.tsx,page.test.tsx,applications/}` into `app/(staff)/`; create `(staff)/layout.tsx`, stub pages, `src/features/shell/**` |
| Portal | move `apps/borrower-portal/src/app/{page.tsx,page.test.tsx}` into `app/(portal)/`; create `(portal)/layout.tsx`, stub pages, `src/features/shell/**` |
| UI | create `packages/ui/src/components/{Pagination,Select,MultiSelect,Drawer,EmptyState,Toast}/`, `utils/useFocusTrap.ts`; modify `Overlay.tsx`, `index.ts`, `index.test.ts`; gallery entries in `apps/lo-console/src/app/gallery/page.tsx` |
| Infra | modify `scripts/worktree-env.sh`, `infra/docker-compose.yml`, `.env.example` |
| e2e | create `e2e/lo-console/shell.spec.ts`, `e2e/borrower-portal/shell.spec.ts` |
| Generated | `packages/api-client` via `make api-client` |

## Tasks

| Task | Description | Depends on | Owned files | Test(s) |
| --- | --- | --- | --- | --- |
| T1 | Settings + `core/clock.py` | — | `core/config.py`, `core/clock.py` | `core/tests/test_clock.py` (default UTC now; `CLOCK_NOW` override; naive → UTC) |
| T2 | Migration + models (E1) | — | migration, model files, `alembic/env.py`, `core/enums.py` | `backend/app/core/tests/test_p5p6_schema.py` (columns, enum defaults, partial unique index, backfill of `consents.status`/`flags.message`); existing `backend/tests/test_schema.py` (FK indexes) |
| T3 | `core/pagination.py` | — | `core/pagination.py` | `core/tests/test_pagination.py` (total, slicing, clamp 0→1 and 500→100, default 25) |
| T4 | `core/storage.py` | T1 | `core/storage.py` | `core/tests/test_storage.py` (stub client unit test; live MinIO round trip, skipped if unreachable) |
| T5 | `least_loaded_lo_id` | — | `applications/assignment.py`, `auth/borrower/service.py` | `applications/tests/test_assignment.py` (fewest active; withdrawn/closed ignored; alphabetical tie; none → `None`); `test_signup.py` still green |
| T6 | Flag messages (E8) | T2 | `verification/{rules,service}.py` | `verification/tests/test_flag_messages.py` (every rule has a message; `write_flag` default + refresh); workflow persona tests assert Aisha's and Ben's messages |
| T7 | Pipeline skip-import (E14) | T2 | `workflows/*` | `workflows/tests/test_portal_source_skips_import.py` (portal app → priced, `import_from_los` never called); existing persona tests stay green |
| T8 | Seed E17 | T2, T5 | `seed/loader.py`, `seed/reset.py` | `seed/tests/test_no_application_borrower.py` (created when password set, skipped when unset, idempotent) |
| T9 | UI primitives (E7) | — | `packages/ui/**` | one Vitest per component + barrel test |
| T10 | LO `(staff)` shell | T9 | `apps/lo-console/**` | `StaffSessionProvider.test.tsx`, `StaffShell.test.tsx` (admin sees admin links, LO does not; nav links), `AdminGuard.test.tsx`, stub page tests; `e2e/lo-console/shell.spec.ts` |
| T11 | Portal `(portal)` shell | T9 | `apps/borrower-portal/**` | `BorrowerSessionProvider.test.tsx`, `PortalShell.test.tsx`, stub page tests; `e2e/borrower-portal/shell.spec.ts` (375 px no h-scroll) |
| T12 | worktree-env slots 11–23 + Valkey databases | — | `scripts/worktree-env.sh`, `infra/docker-compose.yml` | `bash scripts/worktree-env.sh 11` run; guard message for an out-of-range db |
| T13 | api-client regen, e2e, docs | all | `packages/api-client`, `e2e/**` | `make api-client`, full Playwright run |

## Wave schedule (stage 3)

Executed by one worker in sequence (no sub-agents), so file ownership never
overlaps.

| Wave | Tasks | Why this order |
| --- | --- | --- |
| 1 | T1, T2, T3, T5, T12 | Settings, schema and pure helpers first |
| 2 | T4, T6, T7, T8 | Depend on settings/schema |
| 3 | T9 | UI primitives before the shells use them |
| 4 | T10, T11 | App shells |
| 5 | T13 | Generated client, e2e, evidence |

## Acceptance → test map

| Deliverable | Test |
| --- | --- |
| 1 Migration + models | `test_p5p6_schema.py`; `test_schema.py`; `alembic heads` = 1 |
| 2 clock | `test_clock.py` |
| 3 storage | `test_storage.py` |
| 4 pagination | `test_pagination.py` |
| 5 assignment | `test_assignment.py`, `test_signup.py` |
| 6 flag messages | `test_flag_messages.py`, `test_application_pipeline_personas.py` (Aisha/Ben) |
| 7 skip-import | `test_portal_source_skips_import.py` |
| 8 seed E17 | `test_no_application_borrower.py`; `make demo-reset` < 60 s |
| 9 settings | `test_clock.py`, `test_config_defaults` in `test_p5p6_schema.py` |
| 10 LO shell | Vitest shell tests; `e2e/lo-console/shell.spec.ts` |
| 11 portal shell | Vitest shell tests; `e2e/borrower-portal/shell.spec.ts` |
| 12 UI primitives | `packages/ui` Vitest; gallery |
| 13 slots | script run + guard |
| 14 api-client / e2e | `make api-client`; full Playwright run |

## Progress

- [ ] T1 – T13 (ticked in the post-dev section below)
