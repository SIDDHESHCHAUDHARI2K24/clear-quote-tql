# CQ-029 — Handoffs

Append one entry per handoff, newest at the bottom. A new session reads spec.md, plan.md, then the latest entry.

## Handoff 1 — 2026-09-25 — Claude (Sonnet 5)

- **Branch / last commit:** `cq-029-timeline-outbox-panel` @ WIP commit about to be made (not yet committed as of this entry — see "Verify state" to confirm what actually landed)
- **Stage:** 6 Review (findings received and mostly fixed) → 5 Test (re-verification in progress) when the context cap hit. Not yet at 7 Verify or 8 Commit/PR.
- **Done — all four screens are implemented and were passing before the review-fix round:**
  - **Timeline**: `backend/app/features/applications/timeline/{router,schemas,service}.py` + `tests/`. `GET /api/v1/applications/{id}/activity`, paginated, actor resolution (system/staff/borrower), `describe_event()` human messages for every known type + a safe fallback for unknown ones. Frontend: `apps/lo-console/src/features/activity/` (`ActivityTimeline`, `icons.tsx`, tests), wired into `WorkspaceHeader.tsx` via an "Activity" button + `@cq/ui` `Drawer`.
  - **Outbox**: `backend/app/features/notifications/outbox/{router,schemas,service}.py` + `tests/`. `GET /api/v1/outbox` (search/type filter/pagination), `GET /api/v1/outbox/{id}`, `GET /api/v1/outbox/{id}/attachments/{key}` (streams via `core/storage`). Type is derived from subject (plan.md decision 1, no migration). Frontend: `apps/lo-console/src/features/outbox/` (`OutboxList`, `OutboxDetail`, tests) + `(staff)/outbox/page.tsx` (search, type filter, `?email_id=` deep link, sandboxed iframe detail).
  - **Integrations**: `backend/app/features/admin/integrations/{router,schemas,service}.py` + `tests/`. `GET`/`PUT /api/v1/admin/integrations`, Admin-only, reuses CQ-009's `failure_toggle`/`IntegrationCall`. Frontend: `apps/lo-console/src/features/admin/integrations/` (`IntegrationsPanel`, tests) + `(staff)/admin/integrations/page.tsx`. "Run stale check now" hidden behind `STALE_CHECK_JOB_AVAILABLE = false` (plan.md decision 8).
  - **Settings**: `backend/app/features/admin/settings/{router,schemas,service}.py` + `tests/`. `GET /api/v1/admin/settings`, read-only. Frontend: `apps/lo-console/src/features/admin/settings/` (`SettingsPanel`, tests) + `(staff)/admin/settings/page.tsx`.
  - `backend/app/core/registry.py`: 4 new router lines. `packages/api-client` regenerated (`make api-client`) and committed.
  - `backend/app/workflows/tests/test_admin_forced_pricing_failure.py`: AC4's full round trip against the real Temporal test env (force PricingClient to fail → `needs_attention` + named error → unforce + `resume` signal → `priced`). Lives outside owned files, logged as a small necessity in plan.md.
  - `e2e/lo-console/integration-panel.spec.ts` (3 specs, all passing as of the last full e2e run) + `e2e/lo-console/shell.spec.ts` edited (its old stub-text assertions for Outbox/Integrations no longer apply now that CQ-029 built the real pages).
  - `docs/backlog/CQ-029-timeline-outbox-panel/{plan.md,post-dev.md}` written; `docs/backlog/README.md` status row set to "In Review"; `graphify update .` run (graphify-out/ changes are staged/uncommitted, see below).
  - Full e2e run on slot 15 (18/18 passed, including all CQ-029 specs) and a clean full backend suite run (515 passed) were both achieved earlier in the session, before the review-fix round below.
- **In progress — code review (fresh subagent, `git diff HEAD` vs `origin/phase-p5-p6`) returned 10 verified findings; fixes applied but not fully re-verified end to end:**
  1. `ActivityTimeline.tsx` day-bucketing used UTC (`toISOString().slice(0,10)`) instead of the viewer's local calendar date — **fixed** (`localDateKey()`), plus a regression test (`ActivityTimeline.test.tsx`, pinned `TZ=America/New_York` + `vi.setSystemTime`) that I manually verified fails on the old code and passes on the new. **Verified passing** (ran directly).
  2. `IntegrationsPanel.tsx`'s `toggle()` had no try/catch/finally — a rejected promise left the checkbox stuck disabled with an un-reverted optimistic update — **fixed** + regression test. **Verified passing** (ran directly).
  3. Outbox attachment route called sync `stream_object()` directly in the route handler, blocking the event loop for the GET round trip — **fixed**: switched to `astream_object()`, pulling the first chunk eagerly (before constructing `StreamingResponse`) so a missing key still 404s cleanly instead of aborting mid-stream after headers are sent. New test `test_outbox_attachment_stream_404s_cleanly_for_a_missing_object`. **Verified passing** (ran directly against live MinIO, 8/8 outbox tests green).
  4. Outbox free-text search (`q`) built an unescaped `ILIKE` pattern — a literal `%`/`_` in the search text acted as a SQL wildcard — **fixed** (`_escape_like()` + `escape=` kwarg) + regression test `test_outbox_search_escapes_like_wildcards`. **Verified passing**.
  5. `list_outbox` selected the whole `OutboxEmail` entity (including `html`) for every list row just to discard it — **fixed**: now selects only the columns `OutboxEmailRow` needs (`_LIST_COLUMNS`, new `_row_to_out()` mapper). **Verified passing** (same 8/8 outbox run covers this — `list_outbox`'s query shape changed, all list/filter tests still pass).
  6. `admin/settings/service.py` hand-copied the down-payment default literals instead of importing `pricing/scenarios/service.py`'s real (underscore-prefixed) constants — **fixed**: now imports `_DEFAULT_DOWN_PAYMENT_PRIMARY`/`_INVESTMENT` directly. **Verified passing** (9/9 admin tests, including `test_settings_match_snapshot`).
  7. `timeline/service.py`'s `_payload_summary` sliced "first 5" keys from a payload read back from JSONB, which doesn't preserve insertion order — **fixed**: `sorted(payload.items())[:5]` for determinism. **Not re-run in isolation since the fix (trivial, low risk) — should still pass the existing timeline suite; the last targeted timeline test run was in flight when the context cap hit (see below).**
  8. `admin/integrations/service.py`'s `list_integration_status` made ~27 sequential DB/Valkey round trips (2 queries × 9 adapters + 9 Valkey GETs, all `await`ed one at a time) — **fixed**: one `DISTINCT ON` query for the latest call per adapter, one `GROUP BY` query for hourly counts (both safe to run sequentially on the single `AsyncSession`), plus `asyncio.gather` over the 9 Valkey `is_forced_to_fail` calls (safe — `redis.asyncio.Redis` pools its own connections, unlike `AsyncSession`). Also fixed a `SADeprecationWarning` this surfaced (`select().distinct(col)` → `select().ext(distinct_on(col))`, SQLAlchemy 2.1's new API). **Verified passing** (9/9 admin tests, zero warnings).
  9. `OutboxList.tsx` fired a full `GET /outbox` on every keystroke in the search box — **fixed**: added a 300ms debounce (`qInput` shown immediately, `q` — what drives the fetch — debounced) + regression test asserting exactly one fetch for a whole typed string. **Verified passing**.
  10. Outbox `type` derived from subject copy (plan.md decision 1) — **left as-is, already a logged, deliberate tradeoff** (not a code change; see plan.md decision 1 and post-dev.md follow-ups).
  - Two items the reviewer listed as "not in the top 10, for your judgement" (outbox scoping reimplementing `scope_applications`, `STALE_CHECK_JOB_AVAILABLE` needing a manual flip) were **not** acted on — logged as accepted, minor.
- **What's still pending (the next session should do these, in order):**
  1. **Verify the WIP commit landed cleanly** (see "Verify state" below) — this handoff is being written under a hard context-cap stop, so confirm nothing got half-written.
  2. Re-run the **full** targeted suite for the 4 changed backend modules together in one `pytest` invocation (I only ran `backend/app/features/notifications/outbox`, `backend/app/features/admin`, and `backend/app/features/applications/timeline` mostly individually due to heavy concurrent-agent load on the shared Postgres/Temporal causing spurious timeouts/flakes when run combined — see phase-p5-p6-foundation.md's own precedent for this exact flake pattern). Also re-run `backend/app/workflows/tests/test_admin_forced_pricing_failure.py` (was green before the review-fix round; nothing in the review-fix round touched workflow code, so it should still be green, but hasn't been re-run since).
  3. Run the **full** `uv run pytest backend seed` and `pnpm -r run test` once cleanly (both were green mid-session before the review-fix round; re-confirm after).
  4. Run `ruff check backend`, `ruff format --check backend`, `mypy backend/app backend/conftest.py backend/tests backend/scripts`, `pnpm -r run lint`, `pnpm -r run typecheck`, `pnpm exec prettier --check .` once more across the whole repo (all were individually re-checked per touched file during the review-fix round, but not as one final `make lint`-equivalent pass).
  5. Re-run `npx react-doctor -y --blocking error` in `apps/lo-console` (was 0 errors / 4 warnings before the review-fix round — 2 pre-existing, 2 new-but-assessed-as-false-positive on the locale-formatting rule, documented in post-dev.md; the review-fix round didn't touch those call sites again, so should be unchanged, but re-confirm).
  6. Re-run the full e2e suite on slot 15 (`SEED_STAFF_PASSWORD=... LO_BASE_URL=http://localhost:3115 PORTAL_BASE_URL=http://localhost:3215 pnpm exec playwright test e2e/lo-console --workers=1`) — was 18/18 green before the review-fix round, but the review-fix round changed `IntegrationsPanel.tsx` (optimistic-update/try-catch rewrite) and `OutboxList.tsx` (debounce), both exercised by `integration-panel.spec.ts`; re-verify against a live API/worker/LO-console (slot 15, `make demo-reset` first).
  7. Finish **stage 6** formally: no critical/major findings should remain open after the fixes above — update `post-dev.md`'s "Review findings (stage 6)" table (currently says "Pending") with the 10 findings and their resolutions (the detail is in this handoff entry and in code comments at each fix site — search each changed file for "code review finding" / "Code review finding").
  8. **Stage 7**: re-verify the acceptance checklist in `post-dev.md` still holds (nothing in the review-fix round should have changed AC behavior, only correctness/perf under the hood — but AC4's e2e and AC2/AC3's outbox behavior are worth a fresh look given the outbox query and attachment-stream rewrites).
  9. **Stage 8**: squash/organize the WIP commit(s) into clean conventional commits if desired (not required — a single `CQ-029: wip: ...` commit is acceptable to build on), then `gh pr create --base phase-p5-p6 --title "CQ-029: Timeline, Outbox, Integration panel and Settings"` with a body summarizing each AC + evidence (post-dev.md has the material). Post a Kaneo comment on task `j8prs6rvebstcxromc0hu8rv` with the PR link; do not change its status (orchestrator does that).
  10. `graphify-out/` has uncommitted changes from an earlier `graphify update .` run in this session — either commit them with the WIP commit (already done, see below) or re-run `graphify update .` once more right before the final PR-ready commit if more files changed since.
- **Open questions / blockers:** none blocking — everything above is mechanical re-verification + finishing stage 6/7/8 paperwork, not new design decisions. The one thing worth a second pair of eyes: `backend/app/features/admin/settings/service.py` now imports two underscore-prefixed ("private") names from `pricing/scenarios/service.py` (`_DEFAULT_DOWN_PAYMENT_PRIMARY`/`_INVESTMENT`) across a feature-package boundary — deliberate (single source of truth, code review finding 6), ruff's enabled rule set (`E,F,I,UP,B`) doesn't flag it, but flag it in the PR description in case the human wants a public alias instead.
- **Verify state:**
  1. `cd` to this worktree; `git log --oneline -5` and `git status --porcelain` — confirm a `CQ-029: wip: ...` commit exists and the tree is clean (or see what's still uncommitted).
  2. `git push -u origin cq-029-timeline-outbox-panel` if not already pushed (the handoff-writing session should have done this as its last step).
  3. `bash scripts/worktree-env.sh 15` (bash, not zsh) to restore/refresh the `.env` slot-15 config (DBs `cq_dev_s15`/`cq_test_s15`, Valkey db 17, ports 8115/3115/3215, queue `cq-s15`) if a fresh session's worktree doesn't already have it.
  4. `uv run pytest backend seed` and `pnpm -r run test` from the repo root — both should be green (see "What's still pending" #2-3 for why a combined single-shot run is the thing to actually confirm, given the individual-module runs already done).
