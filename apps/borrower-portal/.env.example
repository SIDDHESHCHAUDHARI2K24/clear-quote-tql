NEXT_PUBLIC_API_URL=http://localhost:8000
# CQ-034: the login page's "Need help signing in?" line. Keep this in sync
# with the backend's SUPPORT_INBOX (repo-root .env.example) when either is
# overridden. Optional -- defaults to support@tql.local.
# NEXT_PUBLIC_SUPPORT_EMAIL=support@tql.local
